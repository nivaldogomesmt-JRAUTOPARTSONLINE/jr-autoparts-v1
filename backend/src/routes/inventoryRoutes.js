// src/routes/inventoryRoutes.js — STUB temporario
// Router vazio — Express passa pro proximo middleware se nao tiver match.
// Resultado: 404 pra qualquer rota /api/inventory, mas nao bloqueia outras rotas.
const express = require('express');
const router = express.Router();
module.exports = router;
