// src/routes/botsRoutes.js — STUB temporario
// Router vazio — Express segue pra proxima middleware se nao tiver match.
const express = require('express');
const router = express.Router();
module.exports = router;
